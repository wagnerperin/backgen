{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "keywords": [{{#each keywords}}{{#if @last}}"{{this}}"{{else}}"{{this}}", {{/if}}{{/each}}],
  "author": "{{author}}",
  "license": "{{license}}",
  "dependencies": {
    "body-parser": "^1.19.0",
    "consign": "^0.1.6",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.8.11"
  }
}
